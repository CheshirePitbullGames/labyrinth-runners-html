{
  "name": "maze-adventure-game",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@10.9.4",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "clean:shared": "rm -f packages/shared/tsconfig.tsbuildinfo && rm -rf packages/shared/dist",
    "build": "npm run clean:shared && npm run build --workspace @maze/shared && npm run build --workspace @maze/server && npm run build --workspace @maze/client",
    "typecheck": "npm run clean:shared && npm run typecheck --workspace @maze/shared && npm run typecheck --workspace @maze/server && npm run typecheck --workspace @maze/client",
    "dev:server": "npm run clean:shared && npm run build --workspace @maze/shared && npm run dev --workspace @maze/server",
    "dev:client": "npm run clean:shared && npm run build --workspace @maze/shared && npm run dev --workspace @maze/client",
    "dev": "npm run clean:shared && npm run build --workspace @maze/shared && npm run dev --workspace @maze/server"
  }
}
